# API Documentation

Базовий URL: `http://localhost:3000/api`

## 📅 Графіки (Schedules)

### Отримати останній графік
Повертає найактуальніший графік відключень.

**Request:**
`GET /schedules/latest`

**Response:**
```json
{
  "success": true,
  "date": "2025-11-20",
  "queues": [...]
}
```

### Отримати список доступних дат
Повертає список всіх дат, для яких є збережені графіки.

**Request:**
`GET /schedules/dates`

**Response:**
```json
{
  "success": true,
  "dates": ["2025-11-20", "2025-11-19"]
}
```

### Отримати графік на конкретну дату
**Request:**
`GET /schedules/:date`
* `date` - формат YYYY-MM-DD

### Отримати графік для конкретної черги
**Request:**
`GET /schedules/:date/queues/:queue`
* `date` - формат YYYY-MM-DD
* `queue` - номер черги (наприклад, 1.1)

## 🔄 Оновлення (Updates)

### Запустити примусове оновлення
Запускає парсер для перевірки нових повідомлень в Telegram каналі.

**Request:**
`POST /updates/trigger`

### Отримати історію оновлень
**Request:**
`GET /updates/recent`
* `limit` (optional) - кількість записів (default: 10)

## 📚 Swagger UI
Інтерактивна документація доступна за адресою:
`http://localhost:3000/api-docs`
