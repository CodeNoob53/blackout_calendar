# 📖 API Documentation

**Базовий URL (Production):** `https://blackout-calendar.onrender.com/api`
**Базовий URL (Development):** `http://localhost:3000/api`

**Версія:** 2.0.0

---

## 🌍 Інтернаціоналізація (i18n)

API підтримує дві мови: **українську** (за замовчуванням) та **англійську**.

### Способи вибору мови:

1. **Query параметр** (вищий пріоритет):
   ```
   GET /api/schedules/latest?lang=en
   ```

2. **Заголовок Accept-Language**:
   ```
   Accept-Language: en-US,en;q=0.9,uk;q=0.8
   ```

### Приклад відповіді з помилкою:
**Українська:**
```json
{
  "success": false,
  "error": "Невірний формат дати. Використовуйте YYYY-MM-DD"
}
```

**Англійська:**
```json
{
  "success": false,
  "error": "Invalid date format. Use YYYY-MM-DD"
}
```

---

## 🛡️ Rate Limiting

API захищено від перевантаження за допомогою rate limiting. Ліміти встановлені для різних груп ендпоінтів:

| Група ендпоінтів | Ліміт | Вікно часу |
|------------------|-------|------------|
| `/api/schedules/*` | 200 запитів | 15 хвилин |
| `/api/updates/*` | 60 запитів | 15 хвилин |
| Загальний (всі `/api/*`) | 300 запитів | 15 хвилин |

### Заголовки відповіді:
```
RateLimit-Limit: 200
RateLimit-Remaining: 199
RateLimit-Reset: 1732269600
```

### При перевищенні ліміту:
**Status:** `429 Too Many Requests`
```json
{
  "success": false,
  "error": "Забагато запитів. Спробуйте пізніше."
}
```

---

## 📅 Графіки (Schedules)

### 1. Отримати останній доступний графік

Повертає найактуальніший графік (пріоритет: сьогоднішній → найновіший).

**Endpoint:** `GET /api/schedules/latest`

**Параметри:** Немає

**Відповідь (200 OK):**
```json
{
  "success": true,
  "date": "2025-11-22",
  "queues": [
    {
      "queue": "1.1",
      "intervals": [
        {
          "start": "00:00",
          "end": "03:00"
        },
        {
          "start": "09:00",
          "end": "12:00"
        },
        {
          "start": "18:00",
          "end": "21:00"
        }
      ]
    },
    {
      "queue": "1.2",
      "intervals": [
        {
          "start": "03:00",
          "end": "06:00"
        },
        {
          "start": "12:00",
          "end": "15:00"
        },
        {
          "start": "21:00",
          "end": "24:00"
        }
      ]
    }
  ]
}
```

**Помилки:**
- `404` - Графіки відсутні

---

### 2. Перевірити доступність графіка на сьогодні

**Endpoint:** `GET /api/schedules/today/status`

**Параметри:** Немає

**Відповідь (200 OK) - доступний:**
```json
{
  "success": true,
  "available": true,
  "message": "Графік на сьогодні доступний",
  "today": "2025-11-22"
}
```

**Відповідь (200 OK) - недоступний:**
```json
{
  "success": true,
  "available": false,
  "message": "Графік на сьогодні ще не опублікований"
}
```

---

### 3. Отримати список доступних дат

Повертає всі дати, для яких є збережені графіки.

**Endpoint:** `GET /api/schedules/dates`

**Параметри:** Немає

**Відповідь (200 OK):**
```json
{
  "success": true,
  "dates": [
    "2025-11-22",
    "2025-11-21",
    "2025-11-20"
  ]
}
```

---

### 4. Отримати графік на конкретну дату

**Endpoint:** `GET /api/schedules/:date`

**Параметри шляху:**
- `date` (обов'язковий) - дата у форматі `YYYY-MM-DD`

**Приклад:** `GET /api/schedules/2025-11-22`

**Відповідь (200 OK):**
```json
{
  "success": true,
  "date": "2025-11-22",
  "queues": [
    {
      "queue": "1.1",
      "intervals": [
        {
          "start": "00:00",
          "end": "03:00"
        },
        {
          "start": "09:00",
          "end": "12:00"
        },
        {
          "start": "18:00",
          "end": "21:00"
        }
      ]
    },
    {
      "queue": "2.1",
      "intervals": [
        {
          "start": "06:00",
          "end": "09:00"
        },
        {
          "start": "15:00",
          "end": "18:00"
        }
      ]
    }
  ]
}
```

**Помилки:**
- `400` - Невірний формат дати
- `404` - Графік для вказаної дати не знайдено

---

### 5. Отримати метадані графіка

Повертає інформацію про час останнього оновлення та кількість змін.

**Endpoint:** `GET /api/schedules/:date/metadata`

**Параметри шляху:**
- `date` (обов'язковий) - дата у форматі `YYYY-MM-DD`

**Приклад:** `GET /api/schedules/2025-11-22/metadata`

**Відповідь (200 OK):**
```json
{
  "success": true,
  "date": "2025-11-22",
  "metadata": {
    "date": "2025-11-22",
    "source": "telegram",
    "lastUpdated": "2025-11-21T18:30:00.000Z",
    "firstPublished": "2025-11-21T18:00:00.000Z",
    "updateCount": 2,
    "changeType": "modified",
    "sourcePostId": 12345,
    "messageDate": "2025-11-22"
  }
}
```

**Помилки:**
- `400` - Невірний формат дати
- `404` - Метадані не знайдено

---

### 6. Отримати останній графік для конкретної черги

**Endpoint:** `GET /api/schedules/queues/:queue/latest`

**Параметри шляху:**
- `queue` (обов'язковий) - номер черги у форматі `X.X` (наприклад, `1.1`)

**Приклад:** `GET /api/schedules/queues/1.1/latest`

**Відповідь (200 OK):**
```json
{
  "success": true,
  "queue": "1.1",
  "date": "2025-11-22",
  "intervals": [
    {
      "start": "00:00",
      "end": "03:00"
    },
    {
      "start": "09:00",
      "end": "12:00"
    },
    {
      "start": "18:00",
      "end": "21:00"
    }
  ]
}
```

**Помилки:**
- `400` - Невірний формат черги
- `404` - Графік для черги не знайдено

---

### 7. Отримати графік для конкретної черги на дату

**Endpoint:** `GET /api/schedules/:date/queues/:queue`

**Параметри шляху:**
- `date` (обов'язковий) - дата у форматі `YYYY-MM-DD`
- `queue` (обов'язковий) - номер черги у форматі `X.X`

**Приклад:** `GET /api/schedules/2025-11-22/queues/1.1`

**Відповідь (200 OK):**
```json
{
  "success": true,
  "date": "2025-11-22",
  "queue": "1.1",
  "intervals": [
    {
      "start": "00:00",
      "end": "03:00"
    },
    {
      "start": "09:00",
      "end": "12:00"
    },
    {
      "start": "18:00",
      "end": "21:00"
    }
  ]
}
```

**Помилки:**
- `400` - Невірний формат дати або черги
- `404` - Графік не знайдено

---

## 🔔 Оновлення (Updates)

### 8. Отримати нові графіки

Повертає графіки, які були додані за останні N годин (тільки майбутні дати).

**Endpoint:** `GET /api/updates/new`

**Query параметри:**
- `hours` (опціональний) - кількість годин назад (за замовчуванням: 24, мін: 1, макс: 720)

**Приклад:** `GET /api/updates/new?hours=48`

**Відповідь (200 OK):**
```json
{
  "success": true,
  "hours": 48,
  "count": 2,
  "schedules": [
    {
      "date": "2025-11-23",
      "source": "telegram",
      "publishedAt": "2025-11-22T10:00:00.000Z",
      "messageDate": "2025-11-22",
      "sourcePostId": 12346,
      "pushMessage": "Доступний графік за 23 листопада\n Джерело: офіційний Telegram канал АТ \"Запоріжжяобленерго\""
    },
    {
      "date": "2025-11-24",
      "source": "telegram",
      "publishedAt": "2025-11-22T11:30:00.000Z",
      "messageDate": "2025-11-22",
      "sourcePostId": 12347,
      "pushMessage": "Доступний графік за 24 листопада\n Джерело: офіційний Telegram канал АТ \"Запоріжжяобленерго\""
    }
  ]
}
```

**Помилки:**
- `400` - Невірне значення параметра `hours`

---

### 9. Отримати змінені графіки

Повертає графіки, які були змінені за останні N годин.

**Endpoint:** `GET /api/updates/changed`

**Query параметри:**
- `hours` (опціональний) - кількість годин назад (за замовчуванням: 24, мін: 1, макс: 720)

**Приклад:** `GET /api/updates/changed?hours=24`

**Відповідь (200 OK):**
```json
{
  "success": true,
  "hours": 24,
  "count": 1,
  "schedules": [
    {
      "date": "2025-11-22",
      "source": "telegram",
      "updatedAt": "2025-11-22T08:45:00.000Z",
      "messageDate": "2025-11-22",
      "sourcePostId": 12345,
      "updateCount": 2,
      "pushMessage": "Увага! Внесено зміни за 22 листопада о 10:45\n Джерело: офіційний Telegram канал АТ \"Запоріжжяобленерго\""
    }
  ]
}
```

**Помилки:**
- `400` - Невірне значення параметра `hours`

---

## 🏠 Кореневі ендпоінти

### Інформація про API

**Endpoint:** `GET /`

**Відповідь (200 OK):**
```json
{
  "status": "ok",
  "message": "Blackout Calendar API ⚡",
  "version": "2.0.0",
  "language": "uk",
  "availableLanguages": ["uk", "en"],
  "endpoints": {
    "schedules": {
      "GET /api/schedules/latest": "???'?????????'?? ?????'???????-?? ???????'?????????? ???????"?-??",
      "GET /api/schedules/today/status": "???????????-?????'?? ???????????-???'?? ???????"?-???? ???? ???????????????-",
      "GET /api/schedules/dates": "???????????? ?????-?: ???????'?????????: ?????'",
      "GET /api/schedules/queues/:queue/latest": "?????'???????-?? ???????"?-?? ???>?? ?????????????'?????- ??????????",
      "GET /api/schedules/:date": "???'?????????'?? ???????"?-?? ???? ?????'?- (YYYY-MM-DD)",
      "GET /api/schedules/:date/metadata": "?????'?????????- ???????"?-???? (?????? ?????????>????????, ???-?>?????-???'?? ?????-??)",
      "GET /api/schedules/:date/queues/:queue": "?"?????"?-?? ???>?? ?????????????'?????- ?????????? ?'?? ?????'??"
    },
    "updates": {
      "GET /api/updates/new": "???????- ???????"?-???? ???? ?????'???????- N ?????????? (?hours=24)",
      "GET /api/updates/changed": "?-???-???????- ???????"?-???? ???? ?????'???????- N ?????????? (?hours=24)"
    }
  },
  "rateLimits": {
    "/api/schedules": "200 запитів / 15 хвилин",
    "/api/updates": "60 запитів / 15 хвилин",
    "general": "100 запитів / 15 хвилин (для всього API)"
  },
  "changes": {
    "v2": [
      "Переписано з використанням класової архітектури",
      "Додано валідацію параметрів запитів",
      "Оновлено структуру URL (більш RESTful)",
      "Покращено обробку помилок",
      "Додано форматування відповідей",
      "Додано rate limiting для захисту API",
      "Додано підтримку двох мов (українська/англійська)"
    ]
  }
}
```

---

### Swagger Документація

**Endpoint:** `GET /api-docs`

Інтерактивна документація API з можливістю тестування ендпоінтів.

---

## ⚠️ Коди помилок

| HTTP код | Опис |
|----------|------|
| 200 | Успішний запит |
| 400 | Невірні параметри запиту (валідація) |
| 404 | Ресурс не знайдено |
| 429 | Перевищено ліміт запитів |
| 500 | Внутрішня помилка сервера |

---

## 🔄 Застарілі ендпоінти (Backwards Compatibility)

Для забезпечення сумісності зі старими версіями API підтримуються redirect'и:

| Старий ендпоінт | Новий ендпоінт | HTTP код |
|-----------------|----------------|----------|
| `GET /api/schedule/:date` | `GET /api/schedules/:date` | 301 |
| `GET /api/schedule/queue/:queue/:date` | `GET /api/schedules/:date/queues/:queue` | 301 |
| `GET /api/latest` | `GET /api/schedules/latest` | 301 |
| `GET /api/dates` | `GET /api/schedules/dates` | 301 |

---

## 📊 Приклади використання

> **Примітка**: У прикладах використовується production URL. Для локального тестування замініть `https://blackout-calendar.onrender.com` на `http://localhost:3000`.

### cURL

```bash
# Отримати останній графік
curl https://blackout-calendar.onrender.com/api/schedules/latest

# Отримати графік англійською
curl https://blackout-calendar.onrender.com/api/schedules/latest?lang=en

# Пошук адреси

# Нові графіки за останні 48 годин
curl https://blackout-calendar.onrender.com/api/updates/new?hours=48
```

### JavaScript (fetch)

```javascript
const API_URL = 'https://blackout-calendar.onrender.com';

// Отримати графік на сьогодні
const response = await fetch(`${API_URL}/api/schedules/today/status`);
const data = await response.json();
console.log(data);

```

### Python (requests)

```python
import requests

API_URL = 'https://blackout-calendar.onrender.com'

# Отримати список дат
response = requests.get(f'{API_URL}/api/schedules/dates')
data = response.json()
print(data['dates'])

# Отримати графік на конкретну дату
date = '2025-11-22'
response = requests.get(f'{API_URL}/api/schedules/{date}')
schedule = response.json()
print(schedule)
```

---

**Останнє оновлення:** 2025-11-23
