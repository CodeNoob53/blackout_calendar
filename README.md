# ⚡ Blackout Calendar API

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Node.js](https://img.shields.io/badge/node.js-v18%2B-green.svg)
![Express](https://img.shields.io/badge/express-v4-lightgrey.svg)

**Blackout Calendar API** — це потужний backend-сервіс, який автоматизує збір та надання інформації про графіки погодинних відключень електроенергії (ГПВ) у Запорізькій області.

Сервіс збирає дані з **двох джерел**:
- Офіційний Telegram-канал [Zaporizhzhyaoblenergo_news](https://t.me/s/Zaporizhzhyaoblenergo_news)
- Офіційний сайт [zoe.com.ua](https://www.zoe.com.ua)

Система автоматично обирає найсвіжіші дані та надає їх через зручний REST API.

## 🌐 Live API

- **API Endpoint**: https://blackout-calendar.onrender.com
- **Swagger Docs**: https://blackout-calendar.onrender.com/api-docs

---

## 🚀 Можливості

### Основні функції
- **🔍 Автоматичний парсинг**: Збір даних з Telegram каналу та офіційного сайту.
- **🔄 Розумне оновлення**: Автоматичний вибір найсвіжіших даних з обох джерел.
- **💾 Надійне зберігання**: Всі дані зберігаються в локальній SQLite базі даних.
- **🔌 RESTful API**: Зручні ендпоінти для отримання графіків по датах та чергах.
- **🛡️ Rate Limiting**: Захист API від перевантаження (різні ліміти для різних ендпоінтів).
- **🌍 i18n підтримка**: Автоматичне визначення мови (українська/англійська) з заголовків або query параметрів.
- **📍 Пошук адрес**: Швидкий пошук вулиць та номерів черг по адресі з підтримкою пагінації.
- **🔔 Відстеження змін**: Моніторинг нових та змінених графіків з позначкою джерела.
- **📚 Документація**: Інтерактивна Swagger документація.

### Нові можливості
- **📄 Пагінація**: Підтримка пагінації для списків дат та пошуку адрес (`?limit=10&offset=0`).
- **✅ DTO Валідація**: Автоматична валідація структур даних за допомогою Data Transfer Objects.
- **🏗️ Чиста архітектура**: Repository Pattern + Service Layer для легкого тестування та підтримки.
- **🧪 Покриття тестами**: Jest тести для критичних компонентів системи.
- **⚡ Оптимізована БД**: Індекси та кешування для швидкого доступу до даних.
- **🔒 Безпека SSL**: Правильна обробка SSL сертифікатів з валідацією для zoe.com.ua.
- **🔄 Sync Engine**: Розумна синхронізація даних з Telegram та Zoe без дублювання та лайнографіків.

## 🛠️ Технологічний Стек

- **Core**: Node.js v18+, Express.js 4.19
- **Database**: SQLite (better-sqlite3) з оптимізованими індексами
- **Parser**: Cheerio, Telegraf
- **Architecture**:
  - Repository Pattern (абстракція БД)
  - Service Layer (бізнес-логіка)
  - DTO Pattern (валідація даних)
- **Testing**: Jest з покриттям коду
- **Tools**: Swagger, ESLint, Prettier
- **Security**: Custom SSL validation, SQL injection protection

## 📖 Документація

Ми підготували детальну документацію для розробників:

- [📥 Встановлення та Запуск](docs/SETUP.md) - Інструкція з розгортання проекту.
- [🔌 API Reference](docs/API.md) - Опис всіх доступних ендпоінтів.
- [🏗️ Архітектура](docs/ARCHITECTURE.md) - Огляд структури та компонентів системи.
- [🔄 Sync Engine](docs/SYNC_ENGINE.md) - Система синхронізації даних з Telegram та Zoe.

## ⚡ Швидкий Старт

```bash
# 1. Клонуйте репозиторій
git clone https://github.com/your-username/blackout-calendar.git

# 2. Встановіть залежності
yarn install

# 3. Налаштуйте оточення
cp .env.example .env

# 4. Запустіть сервер
yarn dev
```

Після запуску відкрийте `http://localhost:3000/api-docs` для перегляду документації.

## 📁 Структура Проекту

```
src/
├── controllers/      # HTTP обробка запитів
├── services/        # Бізнес-логіка + кешування
│   └── SyncEngine.js    # Синхронізація даних Telegram + Zoe
├── repositories/    # Абстракція доступу до БД
├── dto/            # Data Transfer Objects (валідація)
│   ├── BaseDTO.js
│   ├── ScheduleDTO.js
│   ├── AddressDTO.js
│   ├── ResponseDTO.js
│   └── README.md
├── routes/         # API маршрути
├── middleware/     # Express middleware
├── utils/          # Допоміжні функції
├── config/         # Конфігурація та константи
├── scraper/        # Парсери даних
├── cli/            # CLI інструменти
│   └── syncCli.js      # CLI для SyncEngine
└── db.js           # Ініціалізація БД

tests/
├── unit/           # Unit тести
│   └── SyncEngine.test.js
├── dto/            # Тести для DTO
├── services/       # Тести для сервісів
└── repositories/   # Тести для репозиторіїв

docs/
├── API.md          # API документація
├── ARCHITECTURE.md # Архітектура системи
├── SETUP.md        # Інструкція встановлення
└── SYNC_ENGINE.md  # Sync Engine документація
```

## 🧪 Тестування

Проект використовує Jest для unit testing:

```bash
# Запустити всі тести
npm test

# Запустити тести з покриттям
npm test -- --coverage

# Запустити конкретний файл тестів
npm test -- BaseDTO.test.js

# Watch mode для розробки
npm test -- --watch
```

## 🔄 Sync Engine CLI

Sync Engine автоматично об'єднує дані з Telegram та Zoe, видаляючи дублікати та лайнографіки:

```bash
# Початкова синхронізація всіх даних
npm run sync:bootstrap

# Регулярна синхронізація (останні 7 днів)
npm run sync:orchestrator

# Синхронізація конкретної дати
npm run sync:date -- 2024-11-26

# Показати довідку
npm run sync:help
```

Для автоматичного використання Sync Engine додайте в `.env`:

```env
USE_SYNC_ENGINE=true
```

Детальніше: [Sync Engine Documentation](docs/SYNC_ENGINE.md)

## 📄 Приклади API з Пагінацією

### Отримати список дат з пагінацією
```bash
GET /api/schedules/dates?limit=10&offset=0
```

Відповідь:
```json
{
  "success": true,
  "data": {
    "dates": ["2024-01-20", "2024-01-19", ...],
    "pagination": {
      "total": 100,
      "limit": 10,
      "offset": 0,
      "hasMore": true
    }
  }
}
```

### Пошук адрес з пагінацією
```bash
GET /api/addresses/search?q=Перемоги&limit=20&offset=0
```

Відповідь:
```json
{
  "success": true,
  "data": {
    "query": "Перемоги",
    "count": 20,
    "total": 45,
    "addresses": [...],
    "pagination": {
      "limit": 20,
      "offset": 0,
      "hasMore": true
    }
  }
}
```

## 🚢 Деплой на Production

### Render.com (рекомендовано)

1. **Створіть Web Service** на [Render.com](https://render.com)
2. **Налаштуйте Build Command:**
   ```bash
   npm install && npm run build
   ```
3. **Start Command:**
   ```bash
   npm start
   ```

Скрипт `npm run build` автоматично:
- Створює структуру БД
- Запускає bootstrap якщо БД порожня
- Пропускає bootstrap якщо дані вже є

**Environment Variables:**
```env
NODE_ENV=production
TELEGRAM_BOT_TOKEN=your_token
TELEGRAM_CHANNEL_ID=@your_channel
USE_SYNC_ENGINE=true
AUTO_UPDATE_ENABLED=true
AUTO_UPDATE_CRON=*/5 * * * *
```

Детальна інструкція: [docs/RENDER_DEPLOYMENT.md](docs/RENDER_DEPLOYMENT.md)

### Інші платформи

Для деплою на інших платформах (Heroku, DigitalOcean, AWS):

1. Встановіть змінні оточення
2. Запустіть `npm run build` для ініціалізації БД
3. Запустіть `npm start` для старту сервера

## 🤝 Contributing

Ми вітаємо будь-який внесок у розвиток проекту! Будь ласка, створюйте Issues та Pull Requests.

## 📄 Ліцензія

Цей проект розповсюджується під ліцензією MIT. Деталі у файлі [LICENSE](LICENSE).

---
*Розроблено з ❤️ для енергонезалежності.*
